{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "VM Creation Job Configuration",
  "description": "This job is run to create a microk8s cluster for you. It takes your email ID as input and sends you all the details of the cluster.",
  "type": "object",
  "properties": {
    "email": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9._%+-]+@badal\\.com$",
      "title": "Email",
      "description": "Email ID ending with @badal.com"
    },
    "installDevtron": {
      "type": "boolean",
      "default": false,
      "title": "Install Devtron",
      "description": "Specify whether to install Devtron. Default is false."
    }
  },
  "required": ["email"],
  "additionalProperties": false,
  "examples": [
    {
      "email": "yourname@badal.com",
      "installDevtron": false
    }
  ]
}
